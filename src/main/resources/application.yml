spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/demoshop
    username: postgres
    password: postgres
    liquibase:
      enabled: true
    mvc:
      locale: ru
  sql:
    init:
      platform: postgresql
